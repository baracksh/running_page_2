# Node
node_modules/
dist/
.vite/

# Python venv
.venv/
__pycache__/
*.pyc

# Sync state / cached data
# data.db IS tracked: it holds all activities (incl. NRC history imported
# once from GPX). CI's coros_sync adds new COROS activities on top of it.
run_page/data.db-journal
imported.json
GPX_OUT/*.gpx
FIT_OUT/*.fit
!GPX_OUT/.gitkeep
!FIT_OUT/.gitkeep

# Logs
*.log
.dev-server.log

# OS
.DS_Store